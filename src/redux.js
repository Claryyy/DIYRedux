// TODO: accept a reducer as our function argument and setup the basic scaffold for redux

function redux() {
  function subscribe() {}
  function dispatch() {}
  function getState() {}
  return { subscribe, dispatch, getState };
}
